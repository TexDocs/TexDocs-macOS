import XCTest
@testable import TexDocsCollaboration

class TexDocsCollaborationTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TexDocsCollaboration().text, "Hello, World!")
    }


    static var allTests = [
        ("testExample", testExample),
    ]
}
